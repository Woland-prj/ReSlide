import { TContextMenuButton } from '@/types/context_menu_buttons.type'
import { actions_menu_buttons } from './actions_menu_buttons.data'
export const context_menu_buttons: TContextMenuButton[] = [actions_menu_buttons & {
    name: 
}

]
